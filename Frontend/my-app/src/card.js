import React from "react";

function Card(number)

{
    const add = (number) =>number * number;
    ReadableStream.useEffect
    return(
        <div>
            <h2>Card {number}</h2>
        </div>
    )
}

export default Card;